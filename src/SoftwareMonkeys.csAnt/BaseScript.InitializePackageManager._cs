using System;
using SoftwareMonkeys.csAnt.Packages;
using SoftwareMonkeys.csAnt.Packages.Schema;

namespace SoftwareMonkeys.csAnt
{
    public partial class BaseScript
    {
        public void InitializePackageManager (IPackageManager packages)
        {
            if (IsVerbose) {
                Console.WriteLine ("");
                Console.WriteLine ("Initializing package manager...");
                Console.WriteLine ("");
            }

            this.packages = packages;
        }
    }
}

